import type { FC } from 'react';

import PropTypes from 'prop-types';


export const MailList: FC = (props) => {
  
  return (
    <>
    </>
  );
};

MailList.propTypes = {
  currentLabelId: PropTypes.string,
  onSidebarToggle: PropTypes.func
};
